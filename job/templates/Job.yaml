{{ if or (and .Values.global.job.kind (eq .Values.global.job.kind "Job")) (and (not .Values.global.job.kind) (and .Values.kind (eq .Values.kind "Job"))) }}
---
apiVersion: batch/v1
kind: Job
{{ include "job.metadata" .}}
{{ include "job.spec" .}}
  {{- end}}